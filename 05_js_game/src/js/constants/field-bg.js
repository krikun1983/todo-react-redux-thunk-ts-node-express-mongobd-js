import { CANVAS } from './index.js';

const FIELD_BG = {
  size: {
    width: CANVAS.size.width,
    height: CANVAS.size.height,
  },
  position: {
    x: 0,
    y: 0,
  },
}

export default FIELD_BG;