const cvs = document.querySelector('canvas');

const CANVAS = {
  size: {
    width: cvs.width,
    height: cvs.height,
  },
  position: {
    x: 0,
    y: 0,
  }
}

export default CANVAS;